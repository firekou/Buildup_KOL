# docs/11 v1.1 定向複審 · gpt

**問法：** 不重審全文，只判斷「你自己上一輪提出的阻擋條件有沒有真的被解除」，並特別檢查有沒有「用新的編造判準取代舊的編造數字」。
**日期：** 2026-08-22

---

## A. 我的阻擋條件解除了嗎？

1. **線性補償／`credibilityMode` 被平均掉** → **已解除**  
   `credibilityMode` 已上移為 G2 gate，`screeningScore` 改為 `fit/pillar/homophily`，且 UI 降格為分帶，短板並列顯示。

2. **等權與 `impact=high` 的循環論證** → **已解除**  
   `impact` 拆成 `decisionRole/share/canVetoAlone`，不再用「佔 25%」反證重要性。

3. **`FLOOR=30` 造成 VETO 選擇偏誤，且未記錄被擋案例** → **部分解除**  
   新增 `EXPERIMENT` 帶與 `vetoLog` 是正確方向；但 `FLOOR ± 10` 的「10」仍是會影響決策的先驗數字。

4. **M3 `pillar > 3`、`3/n` 是編造公式** → **已解除**  
   已從 Zone A 評分刪除，改為 `W-BLURRED-PILLAR` 警示。

5. **G2 與 `R-EMBODIMENT` 漂移、G5 應是驗證而非 gate** → **已解除**  
   VALIDATION/GATES 拆開，G2 共用具身判定，缺 why／缺 mode 回 wizard 補件。

6. **Zone B sandbox 可污染主排序** → **已解除**  
   已改成獨立 `/sandbox`，不得影響主排序，也不得被 `/api/plans/generate` 採用。

7. **`≥30` 被當假校準門檻** → **部分解除**  
   固定 30 已刪，但 §9.4 的統計判準仍未完全可操作化。

8. **B4 七天自動出 B 風險高** → **已解除**  
   改為至少三個完整週週期、人工批准，不再自動出 B。

9. **紅線規則缺漏與關鍵字誤判** → **大致已解除**  
   新增 AI 揭露、捏造來源、冒充真人、高風險建議、仇恨騷擾；關鍵字降為 lint。但第二層 LLM 判定仍需驗收規格。

10. **task 配額 4/2/3/1 與 identity enum 不一致** → **部分解除**  
   配額刪除、identity 納入 enum；但「至少涵蓋 3 種」仍是新先驗數字。

11. **跨域共現誤稱結構洞／橋接** → **已解除**  
   已改名、移入 Zone B，且不得稱橋接或結構洞。

## B. 有沒有「假修正」？

有幾處是**部分假修正**：

- **§2.4 `FLOOR ± 10`**：這個 10 沒有來源，只是把「硬切點」改成「三段硬切點」。好處是已標為 prior，且讓 borderline 可上線；但仍應明確寫成可配置先驗，不得宣稱已校準。
- **§9.4「統計顯著＋R² 實質增量」**：方向正確，但「實質」未定義，`alpha`、模型形式、平台分層、任務分層也未定義。這會讓未來有人主觀判定「看起來實質」。
- **§8.3 至少 3 種 task**：3 仍是產品紀律數字，不是文獻數字。若只是覆蓋提醒可以接受；若阻擋產出，就要標 prior。
- **§5.4 兩層 lint**：不是完全轉嫁，因為把 regex 從最終判定降級是正確的；但 LLM 第二層必須有人工抽樣、誤判紀錄與測試案例，否則只是把不可靠性藏到 prompt 裡。

## C. 不採納理由成立嗎？

27：**不完全成立。** 我上一輪不是主張全面停用 FLOOR，而是要求不要把 `30` 包裝成有根據，且要記錄被擋案例與校準偏誤。新版保留 gate 可以接受；但「Mandler 確立有一條線」不能推出 `30` 或 `±10`。

28：**成立。** 我上一輪本來就主張 `primaryTask` 唯一、`secondaryEffects` 可選；新版採納這點。創作策略需單一，受眾結果不必互斥。

## D. 現在可以開工嗎？

**不能全案開工；可先做 P1／P2。**  
剩下必須先改的一件事：把 §9.4 的「calibrated／出 B」判準寫成可執行規格，尤其定義「統計顯著」與「R² 實質增量」的具體判準。

---

生成模型：`gpt-5.5`（AI Token King MCP）｜tokens：27175｜日期：2026-08-22
