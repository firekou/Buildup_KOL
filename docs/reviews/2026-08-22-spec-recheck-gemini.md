# docs/11 v1.1 定向複審 · gemini

**問法：** 不重審全文，只判斷「你自己上一輪提出的阻擋條件有沒有真的被解除」，並特別檢查有沒有「用新的編造判準取代舊的編造數字」。
**日期：** 2026-08-22

---

**A. 你的阻擋條件解除了嗎？**

1. **底線校準悖論（倖存者偏差）** → **已解除**。你將單一的 VETO 劈開，劃出 `EXPERIMENT` 帶允許發布並強制標記，同時針對 VETO 案強制記錄人工覆核。這在工程上完美打通了低分數據的回收管道，真正的校準才成為可能。
2. **用假公式 `3/n` 取代假權重** → **已解除**。徹底刪除，退回警告，這是對 §0 紀律的確實遵守。
3. **硬性配額 4/2/3/1 逼迫 AI 幻覺** → **已解除**。刪除無依據的定額，改為最低涵蓋種類要求。
4. **線性補償與等權逃避** → **已解除**。把 `credibilityMode`（類別變數）抽離成 Gate 2 是極度正確的決定。後續的三維度等權雖仍是假設，但你把總分降格為「分帶」並並列三維長條，拔掉了精確預測的假象，這在零數據期是負責的做法。
5. **Zone B 隔離與自動出 B** → **已解除**。移至 `/sandbox`、反灰不顯示數值、改為 3 個完整週週期與人工批准，徹底封死了定錨效應與週期誤差。
6. **紅線模型投票與關鍵字假陽性** → **已解除**。廢除模型投票改採客觀標準、新增 `R-FABRICATED-SOURCE` 補上資料庫型致命傷，並將正規表達式降格為 lint，把語意判斷還給 Claude，這正是 skill 該做的事。

---

**B. 有沒有「假修正」？**

你這次整體算誠實，沒有把文獻瞎掰成新的數字，但以下幾點仍需釐清：

*   **§2.4 的 EXPERIMENT 帶（`± 10`）：** 這是先驗的工程設定，不是假修正。只要在 UI 和 `scoring-config.json` 裡清楚標示這是一個供實驗的 `experimentBandWidth`，而不是宣稱「文獻說誤差是 10」，這在軟體工程上就完全合法。
*   **§9.4 的「對 R² 有實質增量」：** **這是個標準的假判準（偷埋漏洞）。**「實質」這個詞在統計實作上毫無意義，工程師寫扣時到底要 `> 0.01` 還是 `> 0.05`？這等於把編造數字的權力從規格書推給了寫扣的人。你必須在常數檔裡定死一個明確的增益門檻（例如 `r2_delta_threshold: 0.05`）並標註為先驗，否則這條出 B 條件無法實作。
*   **§8.3 的「至少涵蓋 3 種 primaryTask」：** 這是系統的 UX 規則，目的是逼迫使用者嘗試不同切角，不涉及「宣稱理論如此」。作為操作限制，它可以接受。
*   **§5.4 的兩層 lint：** **是真解決，不是轉嫁。** 關鍵在於你傳入了 `why_plain` 與 `semantic_prompt`。LLM 本身不具備你的領域知識，但當你把「什麼情況該擋、什麼情況是比喻」的判斷邊界寫清楚讓它執行，這就是把靜態系統升級為語意系統的正確架構。

---

**C. 對方明確不採納你的兩條意見，他的理由成立嗎？**

*   **第 27 條（不全面停用 FLOOR 硬切點）：** **你的理由成立。** 我上一輪要求停用，是因為原本的架構會導致悖論。現在你已經用 `EXPERIMENT` 帶解決了觀測死角，同時保留 VETO 區以阻擋極度惡劣的產出（並留存覆核記錄）。在機制已補齊的前提下，保留 FLOOR 是合理的。
*   **第 28 條（不堅持嚴格單一 task）：** **你的理由成立，我接受 GPT 的修正。** 創作時的 Hook/CTA（`primaryTask`）確實必須互斥以維持銳利度，但觀眾看完後可能同時留言又分享（`secondaryEffects`）。只要你確實用系統卡死了「副作用不得反向改寫 Hook 與 CTA」這條鐵律，多選副作用就不會導致平庸化。

---

**D. 一句話：現在可以開工實作了嗎？**

**可以開工。** 

（附帶條件：進版控前，請把 §9.4 那個打太極的「實質增量」換成常數檔裡具體可被程式執行的 `r2_delta_threshold` 變數，並承認它是先驗）。核心的邏輯炸彈已經全部拆除，這份規格現在是一套嚴謹且具備自我修正能力的系統了。

---

生成模型：`gemini-3.1-pro-preview`（AI Token King MCP）｜tokens：26563｜日期：2026-08-22
